// extension XCScheme {
//     fileprivate var uri: URI {
//         let escapedName = name.addingPercentEncoding(withAllowedCharacters: .urlHostAllowed) ?? name
//         return try! URI(string: "xcscheme://\(escapedName)")
//     }
// }
